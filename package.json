{
    "name": "autobidder-project",
    "private": true,
    "scripts": {
      "dev": "concurrently \"cd backend && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\" \"cd frontend-app && npm run dev\""
    },
    "devDependencies": {
      "concurrently": "^8.0.0"
    }
  }
  